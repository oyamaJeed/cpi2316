# TARGET NAME
TARGET := display_1
SRC := $(TARGET).c
OBJ := $(TARGET).o
DLSO := libdlink.so

CFLAGS := -g -Wall
LFLAGS := -lm -pthread
CC := gcc

all : $(TARGET)


$(TARGET): $(OBJ) 
	$(CC) -shared $(SRC) -fPIC -o $(DLSO)
.c.o:
	$(CC) -c $^
clean:
	rm -f $(TARGET) $(OBJ)
	rm -f $(DLSO)
